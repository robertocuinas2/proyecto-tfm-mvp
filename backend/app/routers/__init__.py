# Paquete de routers. Cada módulo expone `router` (APIRouter) y se registra en app/main.py.
# Routers de dominio (antes el monolítico frontend_core.py):
#   dashboard, animals, zones, tasks, lactations, alerts, predictions,
#   incidents, treatments, employees, machinery
# Otros: auth, weather, audit, orders, shifts, handovers, health
